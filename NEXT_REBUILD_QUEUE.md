# Next rebuild queue

Generated: 2026-05-09 10:24:43 UTC  
Latest inspected local main SHA: `200fafcc20451cc43e8d6272588ec7e26e12d9c8`  
Current repo score estimate: **7.8 / 10**

Repo is strong pre-production architecture, not 10/10 launch-ready. This queue deliberately avoids raw merges from stale open PRs and requires every implementation branch to start from latest `main`.

## Execution order

| Order | Branch name | Scope | Files likely touched | Migration allowed? | Risk | Acceptance criteria | Validation commands |
| ---: | --- | --- | --- | --- | --- | --- | --- |
| 1 | `chore/close-superseded-open-prs` | Close or label stale docs/governance/duplicate PRs that are represented by merged main and point maintainers to the control room. | GitHub PR comments/labels only; no repo files unless updating control docs. | No | P2 | Stale PRs are commented with the approved templates; PRs with unique content stay open; no PR is merged. | `git diff --check`; GitHub PR audit/export. |
| 2 | `feat/rebuild-provider-runtime-enforcement-latest-main` | Rebuild provider runtime enforcement from latest main; preserve useful intent from PR #95 only after authenticated diff review. | Likely `server/**`, provider services, worker/retry tests, possibly docs. | Yes, only if explicitly reserved as next migration after verification. | P0 | Provider attempts, retry classification, dead-letter behavior, and provider contract enforcement are runtime-backed; no placeholder success; no duplicate worker abstractions; migration verifier passes if a migration is added. | `pnpm run check`; `pnpm test -- --runInBand`; `pnpm run build`; `node scripts/verify-migrations.mjs`; `node scripts/ci-governance-guards.mjs all`. |
| 3 | `fix/rebuild-observability-healthchecks-from-latest-main` | Rebuild observability and healthchecks from latest main; preserve useful intent from PR #91 only after authenticated diff review. | Likely `server/**` health/readiness routes, middleware/logging tests, docs. | No | P1 | Health/readiness endpoints are truthful; logs redact secrets/PHI; no duplicate middleware; checks integrate with current security/provider/worker bootstraps. | `pnpm run check`; `pnpm test -- --runInBand`; `pnpm run build`; `node scripts/ci-governance-guards.mjs all`. |
| 4 | `feat/rebuild-reservation-lifecycle-truth-latest-main` | Resolve reservation lifecycle collision risk from PR #88 only after provider/runtime migration sequencing is reserved. | Likely `server/**`, stock/reservation services/tests, maybe `drizzle/**` if approved. | Yes, only after provider/runtime migration number is reserved or explicitly not needed. | P0 | Canonical availability formula is enforced; release/consume/expire/fail transitions are idempotent; no oversell; DB-backed race proof is available before launch claims. | `pnpm run check`; `pnpm test -- --runInBand`; `TEST_DATABASE_URL=... pnpm test -- server/mysql-concurrency.integration.test.ts`; `node scripts/verify-migrations.mjs`. |
| 5 | `fix/ocr-fake-success-placeholder-storage-paths` | Fix P0 OCR fake-success and placeholder storage paths if still present after current-main inspection. | Likely OCR/storage service files and tests under `server/**`. | No by default. | P0 | OCR never reports success for fake/placeholder persistence; failed storage/provider paths are explicit and auditable; existing placeholder guard coverage remains green. | `pnpm run check`; `pnpm test -- --runInBand`; `pnpm run build`; `node scripts/ci-governance-guards.mjs all`. |
| 6 | `feat/barcode-production-ux-screen-wiring` | Finish barcode production UX screen-level wiring without changing provider/runtime domains. | Likely `client/**` barcode screens/components, route wiring, client tests. | No | P1 | Barcode scan/label workflows are wired to production UX screens; no mock-only success path; scan truth remains guarded. | `pnpm run check`; `pnpm test -- --runInBand`; `pnpm run build`. |
| 7 | `test/mysql-concurrency-proof-with-test-database-url` | Run and harden DB-backed MySQL concurrency proof from PR #89/#90 intent without duplicating harnesses. | Likely `server/*mysql*test.ts`, scripts/docs. | No unless a proven schema gap requires a separate approved feature PR. | P1 | `TEST_DATABASE_URL` run proves reservation/payment/stock race behavior; skipped local runs remain honest and do not claim DB proof. | `TEST_DATABASE_URL=... pnpm test -- server/mysql-concurrency.integration.test.ts`; `pnpm test -- --runInBand`; `pnpm run check`. |
| 8 | `feat/payment-webhook-raw-body-refund-settlement-truth` | Verify raw-body payment webhook route and refund settlement truth. | Likely payment webhook/refund services/tests under `server/**`. | Yes only if an approved settlement ledger gap remains. | P0 | Webhook signature verification uses raw body; settlement/refund state is durable and truthful; no fake provider success. | `pnpm run check`; `pnpm test -- --runInBand`; `pnpm run build`; focused payment/refund guard tests. |
| 9 | `chore/deployment-monitoring-backup-restore-proof` | Produce deployment, monitoring, backup, and restore proof without product feature changes. | Likely scripts/docs/tests for deployment proof. | No | P1 | Release gate, backup, restore, monitoring, and smoke proof are reproducible; environment caveats are explicit. | `pnpm run build`; deployment proof scripts; `pnpm test -- --runInBand`; `node scripts/ci-governance-guards.mjs all`. |
| 10 | `chore/final-ux-admin-investor-dev-audit-pack` | Final UX/admin polish evidence and investor/developer audit pack. | Likely docs, screenshots, admin UX polish if separately approved. | No | P2 | Audit pack truthfully summarizes readiness, blockers, validation, and remaining launch criteria; no 10/10 launch claim until blockers are cleared. | `pnpm run check`; `pnpm test -- --runInBand`; `pnpm run build`; `git diff --check`. |

## Sequencing rules

1. Do not merge any stale open PR raw.
2. Provider runtime enforcement is the next implementation prompt: `feat/rebuild-provider-runtime-enforcement-latest-main`.
3. Observability/healthchecks comes after provider runtime sequencing unless maintainers explicitly confirm it is migration-free and non-conflicting.
4. Reservation lifecycle waits until provider/runtime migration numbering is reserved or confirmed unnecessary.
5. PR #89/#90 concurrency work must be consolidated into one harness; do not keep duplicate race-proof branches.
6. Payment, OCR, and barcode work must not start as part of this governance PR.
