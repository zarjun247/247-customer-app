# NEXT REBUILD QUEUE

This queue is the authoritative order after the open-PR control-room pass. Every implementation branch must start from latest `main`. Do not merge stale branches raw.

## Execution order

| Order | Branch name | Purpose | Likely files touched | Migration allowed? | Risk | Acceptance criteria | Validation commands |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `chore/close-superseded-open-prs` | Close/supersede stale docs, audit, governance, duplicate barcode, duplicate observability, duplicate provider, duplicate reservation, and duplicate MySQL PRs after live confirmation. | GitHub PR comments/labels only; docs may reference `OPEN_PR_CONTROL_ROOM.md`, `OPEN_PR_CLOSURE_COMMENTS.md`, `STALE_PR_STATUS.md`. | No | P2 | Superseded PRs are commented with the correct template and closed; uncertain PRs remain open; no code changes. | `git status --short`; GitHub PR list/export; optional `git diff --check` for docs-only changes |
| 2 | `docs/runtime-stub-fake-success-audit-latest-main` | Merge or rebuild PR `#113` runtime stub/fake-success audit if clean and non-duplicative. | Docs/audit ledgers only; no runtime implementation. | No | P1 | Current fake-success/stub risks are listed with file evidence; no runtime code is changed; launch claims remain honest. | `pnpm run check`; `pnpm test -- --runInBand`; `pnpm run build`; `git diff --check` |
| 3 | `feat/rebuild-provider-runtime-enforcement-latest-main` | Rebuild provider runtime enforcement from latest main; supersedes older provider PR `#95` and latest candidate `#114` branch history. | Provider contracts/services/routes/tests; `drizzle` migration only if current schema needs durable enforcement state; docs updated. | Yes, only after reservation | P0 | Providers fail closed; no fake provider success; migration number reserved from latest main; current tests and provider-specific tests pass. | `pnpm install`; `pnpm run check`; `pnpm test -- --runInBand`; `pnpm run build`; `node scripts/verify-migrations.mjs`; `git diff --check` |
| 4 | `feat/rebuild-observability-healthchecks-latest-main` | Rebuild observability/healthchecks from latest main, preferring latest PR `#117` intent over older `#80`/`#91`. | Healthcheck routes, read-only diagnostics, tests, docs; no provider/reservation feature work. | No unless explicitly justified | P1 | Healthchecks are read-only, actionable, non-leaky, and CI-covered; no migration conflict; older observability PRs can be closed/superseded. | `pnpm run check`; `pnpm test -- --runInBand`; `pnpm run build`; `node scripts/ci-governance-guards.mjs all || true`; `git diff --check` |
| 5 | `feat/rebuild-reservation-lifecycle-truth-latest-main` | Rebuild reservation lifecycle truth only after provider runtime migration number is reserved/merged and DB concurrency proof is understood. | Reservation services/routes/tests; inventory/stock reservation paths; `drizzle` migration if durable lifecycle state is missing. | Yes, only after provider runtime lands | P0 | Reservation transitions are durable, auditable, idempotent, and concurrency-safe; no migration number conflict with provider runtime; stale PR `#88` is superseded. | `pnpm install`; `pnpm run check`; `pnpm test -- --runInBand`; `TEST_DATABASE_URL=... pnpm test -- --runInBand`; `pnpm run build`; `node scripts/verify-migrations.mjs`; `git diff --check` |
| 6 | `fix/ocr-fake-success-placeholder-storage-paths` | Fix P0 OCR fake-success and placeholder storage paths if still present after the audit. | OCR/upload/storage server modules, tests, docs. | No unless durable audit table is explicitly required in a later PR | P0 | OCR cannot report success without real processing/storage truth; placeholder paths are removed or explicitly marked non-production; tests prove fail-closed behavior. | `pnpm run check`; `pnpm test -- --runInBand`; `pnpm run build`; `git diff --check` |
| 7 | `feat/barcode-production-ux-screen-wiring` | Finish barcode production UX screen-level wiring; rebuild useful `#47` component intent without raw stale merge. | Barcode UI screens/components, scanner integration points, client tests, possibly server read endpoints if already supported. | No | P1 | Barcode workflows are usable at screen level, have error states, avoid fake success, and do not regress stock/product identity. | `pnpm run check`; `pnpm test -- --runInBand`; `pnpm run build`; manual/screenshot validation for UI changes; `git diff --check` |
| 8 | `test/mysql-concurrency-proof-db-backed` | Run and extend DB-backed MySQL concurrency proof with `TEST_DATABASE_URL`; close `#89`/`#90` if merged `#116` fully covers them. | Tests/harness docs only unless a real concurrency bug is fixed in a separate implementation PR. | No | P1 | DB-backed concurrency tests run against MySQL; reservation/payment/idempotency edge cases are covered; flaky/local-only assumptions are documented. | `TEST_DATABASE_URL=... pnpm test -- --runInBand`; `pnpm run check`; `pnpm run build`; `git diff --check` |
| 9 | `feat/payment-webhook-refund-settlement-truth` | Implement payment webhook raw-body verification and refund settlement truth; extract any useful `#62` fail-closed intent. | Payment webhook route, refund/settlement services, tests, payment docs. | Maybe, only if durable settlement/audit state is missing | P0 | Webhooks verify raw body signatures, refund settlement truth is durable, payment failures are fail-closed, and tests cover replay/idempotency. | `pnpm install`; `pnpm run check`; `pnpm test -- --runInBand`; `pnpm run build`; `node scripts/verify-migrations.mjs`; `git diff --check` |
| 10 | `chore/deployment-monitoring-backup-restore-proof` | Prove deployment, monitoring, backup, and restore readiness. | Deployment docs, runbooks, backup/restore scripts or tests, monitoring docs. | No unless a later explicit infra-state migration is approved | P1 | Restore drill is documented and reproducible; monitoring alerts are mapped; deployment rollback is proven; no 10/10 launch claim until evidence exists. | `pnpm run check`; `pnpm test -- --runInBand`; `pnpm run build`; backup/restore dry-run command; `git diff --check` |
| 11 | `chore/final-ux-admin-polish-investor-dev-audit-pack` | Final UX/admin polish and investor/developer audit pack after P0 truth gaps close. | Docs, UX polish, admin screens, screenshots, audit package. | No | P2 | Investor/dev pack reflects actual production evidence, remaining risks, and screenshots; no inflated readiness score. | `pnpm run check`; `pnpm test -- --runInBand`; `pnpm run build`; UI screenshot checks if web app changes; `git diff --check` |

## Hard sequencing rules

1. Provider runtime enforcement comes before reservation lifecycle truth.
2. Migration-bearing PRs must reserve numbers from latest main and must not share a migration number.
3. Observability can proceed before reservation if it stays read-only and avoids provider/reservation schema changes.
4. Barcode screen-level UX must not claim production readiness until provider, OCR/storage, inventory, and reservation truth paths are honest.
5. Stale branches are never merged raw; useful intent is rebuilt or extracted from latest main.

## Exact next implementation prompt

`feat/rebuild-provider-runtime-enforcement-latest-main`
