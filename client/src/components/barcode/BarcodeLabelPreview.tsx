import React from "react";
export type LabelJobStatus = "queued"|"printed"|"failed"|"requeued";
export function BarcodeLabelPreview({ payload, status, printerConfigured, onReprint }: { payload: { productName: string; generic?: string | null; batchNo?: string | null; expiryDate?: string | null; mrp?: string | number | null; storeLabel?: string | null; internalBarcode: string; scheduleWarning?: string | null; }; status: LabelJobStatus; printerConfigured: boolean; onReprint?: () => void; }) {
  return <section aria-label="Barcode label preview" className="rounded border p-3 space-y-2"><div className="text-sm font-semibold">{payload.productName}</div>{payload.generic ? <div className="text-xs">{payload.generic}</div> : null}<div className="text-xs">Batch: {payload.batchNo ?? "N/A"}</div><div className="text-xs">Expiry: {payload.expiryDate ?? "N/A"}</div><div className="text-xs">MRP: {payload.mrp ?? "N/A"}</div><div className="text-xs">Store: {payload.storeLabel ?? "N/A"}</div><div className="text-xs">Barcode text: {payload.internalBarcode}</div>{payload.scheduleWarning ? <div className="text-xs text-amber-700">{payload.scheduleWarning}</div> : null}<div className="text-xs">Job status: {status}</div>{!printerConfigured ? <div className="text-xs text-amber-700">Printer not configured. Use browser print fallback.</div> : null}<div className="flex gap-2"><button className="rounded border px-3 py-2" onClick={() => window.print()}>Print</button><button className="rounded border px-3 py-2" onClick={onReprint}>Reprint</button></div></section>;
}
export default BarcodeLabelPreview;
